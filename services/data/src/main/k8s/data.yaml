kind: Service
apiVersion: v1
metadata:
  name: data-service
spec:
  type: NodePort
  selector:
    app: data-service
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 4567
  - name: debug
    protocol: TCP
    port: 5005
    targetPort: 5005
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: data-service
  labels:
    app: data-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      containers:
      - name: data-service
        image: gcr.io/qingyangc-sandbox/hacking-around:data
        imagePullPolicy: Always
        ports:
        - containerPort: 4567
